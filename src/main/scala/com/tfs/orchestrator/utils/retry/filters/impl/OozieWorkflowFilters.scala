package com.tfs.orchestrator.utils.retry.filters.impl

import com.tfs.orchestrator.utils.retry.filters.RetryFilter

class OozieWorkflowFilters extends RetryFilter{
  override def filter(wfIds: List[String]): Unit = {

  }
}
